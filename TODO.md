# 项目规划与待办（TODO）

> 记录尚未实现、但值得后续完善的功能。完成一项后将 `[ ]` 改为 `[x]` 并补充完成日期。

---

## 规划概览

| 阶段 | 周期 | 目标 |
|------|------|------|
| [立即可做](#立即可做12-天) | 1–2 天 | 快速改善体验与可维护性，不改架构 |
| [中期规划](#中期规划与-todo-最契合) | 1–2 周 | 持久化、定时抓取，解决「首次加载慢」根因 |
| [数据与质量](#数据与质量) | 持续迭代 | 内容去重、源健康度、分类与稳定性 |

---

## 立即可做（1–2 天）

### [x] 增量抓取，而不是每次全量

- 高优先级源每 10 分钟、普通源每 30 分钟分批更新
- API 读 SQLite，后台线程定时增量
- **完成日期**：2026-08-29

### [x] 抓取健康度面板

- `/api/feeds/health` + 前端可展开面板（成功/失败/噪音/降权）
- **完成日期**：2026-08-30

### [x] `.gitignore` + 基础运维

- 排除 `venv/`、`__pycache__/`、`data/`、`*.db`
- `scripts/init_db.py` 可初始化数据库
- **完成日期**：2026-08-29

### [x] 标题搜索（轻量版）

- `/api/articles?q=` 标题模糊搜索；筛选栏搜索框（防抖）
- **完成日期**：2026-08-30

---

## 中期规划（与 TODO 最契合）

> 若时间有限，建议按此顺序：**SQLite 持久化 → 后台定时抓取 → 源健康度 + 去重**。

### [x] 抓取结果写入本地轻量数据库（SQLite）

- 文章持久化到 `data/aggregator.db`，按 `link` 唯一键 upsert
- API 优先读库；空库时后台全量，有数据后秒开
- 历史保留约 30 天（`prune_old_articles`）
- **完成日期**：2026-08-29

### [x] 后台定时任务

- 后台线程每分钟检查到期源；高优 10 分钟 / 普通 30 分钟
- **完成日期**：2026-08-29

### [x] 文章去重与合并

- 规范化 link（解开 Google 跳转、去掉 utm）后唯一入库
- Google News 标题清洗尾部「- 域名/媒体名」
- 标题相似度同题合并（`quality.cluster_articles`），卡片展示「同题 N 家来源」
- 噪音标题过滤 + 连续失败/高噪音源自动降权（降权源 6 小时才再抓）
- **完成日期**：2026-08-30

### [ ] 阅读模式增强

- [ ] 展示 RSS `description` 摘要
- [ ] 「仅看今日 / 本周」时间筛选
- [ ] 收藏 / 稍后读（localStorage 即可，无需账号）
- **涉及文件**：`app.py`、`templates/index.html`

### [ ] 订阅导出

- OPML 导出当前信息源列表
- 或生成聚合 RSS（如 `/feed.xml?country=中国`），接入 Reeder 等阅读器
- **涉及文件**：`app.py` 新增路由

### [ ] 多语言界面

- 源名已有中英文，UI 文案支持中/英切换
- **涉及文件**：`templates/index.html`、可选 i18n 字典

---

## 数据与质量

### [ ] 源质量评分与自动降权

- 连续 N 次抓取失败 → 标记「暂不可用」或降低优先级
- 避免列表中大量空源，减少无效请求
- **涉及文件**：`config.py`（可选动态配置）、`app.py`、`fetch_logs` 表

### [ ] 按主题 / Tag 分类

- 在国家、机构之外增加维度：安全、经济、科技、气候等
- 可从 RSS 分类字段或关键词规则打 tag
- **涉及文件**：`config.py`（扩展字段）、`app.py`、前端筛选 UI

### [x] 中国源抓取策略优化

- 人民网/新华/中国日报等优先直连 RSS；无官网 RSS 的智库改为带关键词与 `when:` 的精准 GNews
- 新增外交部发言人、国新办白皮书；补强 WTO/UN/WHO/IEA/BIS/美联储/欧央行与 IMF、世行报告流
- **完成日期**：2026-08-29

### [x] 热点词云 / 本周议题

- 预定义 `TOPIC_LEXICON`，从近 7 日标题统计词频；`/api/topics` + 前端词云筛选
- **完成日期**：2026-08-29

### [ ] 抓取任务队列化（可选）

- 源数量继续增加时，用 Celery / Redis 或轻量队列替代 ThreadPoolExecutor
- 与 DB、定时任务方案配合
- **优先级**：低，当前 200 源规模可暂不引入

---

## 产品级（远期）

### [ ] 简单管理后台

- 网页上增删改信息源，无需手改 `config.py` 并重启

### [ ] 部署与监控

- Docker 化部署 + 健康检查
- 抓取大面积失败时邮件 / Telegram 通知

### [ ] AI 摘要（可选）

- 对每页或每日文章生成一句中文要点
- 有 API 成本，建议作为可选开关

### [ ] 部署到云平台

- Heroku / PythonAnywhere / 云服务器 + Docker

### [ ] 增加邮件订阅功能

---

*最后更新：2026-08-30*
